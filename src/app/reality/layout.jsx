export const metadata = {
    title: "List",
    description: "Shop Items",
};
function layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}

export default layout
